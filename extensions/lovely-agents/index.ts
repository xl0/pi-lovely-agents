import type { ExtensionAPI } from "@earendil-works/pi-coding-agent"

export default function lovelyAgentsExtension(_pi: ExtensionAPI) {
	// Runtime behavior will be added after the design is settled.
}
